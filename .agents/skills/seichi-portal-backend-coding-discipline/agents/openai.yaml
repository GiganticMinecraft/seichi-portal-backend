interface:
  display_name: "seichi Portal Coding"
  short_description: "Use project sqlx, auth, and from_raw_parts rules."
  default_prompt: "Use $seichi-portal-backend-coding-discipline when changing seichi-portal-backend Rust code that touches sqlx queries, repositories, use cases, authorization boundaries, or domain type construction (from_raw_parts)."
